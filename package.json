{
  "name": "valentine-app",
  "version": "1.0.0",
  "description": "A playful Valentine’s Day web app with interactive responses",
  "scripts": {
    "build:css": "sass styles.scss styles.css",
    "build:ts": "tsc script.ts",
    "build": "npm run build:css && npm run build:ts"
  },
  "dependencies": {},
  "devDependencies": {
    "sass": "^1.77.0",
    "typescript": "^5.6.3"
  }
}
